import { MvpPlaceholder } from "@/components/patient/MvpPlaceholder";

export default function PharmacyPage() {
  return (
    <MvpPlaceholder
      title="Pharmacy"
      description="Search by pharmacy name and ZIP or NCPDP, then select where prescriptions should be sent."
      sowRef="§6.5 Pharmacy"
    />
  );
}
